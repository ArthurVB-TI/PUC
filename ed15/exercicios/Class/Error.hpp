using namespace std;

template <class T>
class Ordenar{
    public:

        // Metodos de criação
        void init_Ordenar(){ }
        Ordenar(){ init_Ordenar(); }

        // Metodos de ordenação
        T* push(T* values, T value, int length){
            
        }
        
        // Metodos de leitura
        void index(T* values, int length){
            for(int i = 0; i < length; i = i + 1){
                cout << values[i] << " ";
            }
        }
};